<header>
  <div class="brand">
    <a href="http://cyberscore.me.uk" target="_blank">Cyberscore Notifications</a>
  </div>
  Welcome, <strong>{{ username }}</strong>
</header>

{{#if total }}
{{else }}
  <div class="zero-notifications">
    <h1>No notifications :C</h1>
  </div>
{{/if }}

{{#each notifications }}
  {{> notification-table }}
{{/each }}